diff --git a/lib/encode.c b/lib/encode.c
index 5ea29ff..3f279c7 100644
--- a/lib/encode.c
+++ b/lib/encode.c
@@ -13,8 +13,8 @@
 #include <internal.h>
 
 #include <stdio.h>
-#include <pwd.h>
-#include <grp.h>
+// #include <pwd.h>
+// #include <grp.h>
 #include <sys/types.h>
 
 #ifdef STDC_HEADERS
@@ -157,13 +157,10 @@ th_set_device(TAR *t, dev_t device)
 void
 th_set_user(TAR *t, uid_t uid)
 {
-	struct passwd *pw;
 
-	pw = getpwuid(uid);
-	if (pw != NULL)
-		strlcpy(t->th_buf.uname, pw->pw_name, sizeof(t->th_buf.uname));
+	strlcpy(t->th_buf.uname, "0", sizeof(t->th_buf.uname));
 
-	int_to_oct(uid, t->th_buf.uid, 8);
+	int_to_oct(0, t->th_buf.uid, 8);
 }
 
 
@@ -171,13 +168,9 @@ th_set_user(TAR *t, uid_t uid)
 void
 th_set_group(TAR *t, gid_t gid)
 {
-	struct group *gr;
+	strlcpy(t->th_buf.gname, "0", sizeof(t->th_buf.gname));
 
-	gr = getgrgid(gid);
-	if (gr != NULL)
-		strlcpy(t->th_buf.gname, gr->gr_name, sizeof(t->th_buf.gname));
-
-	int_to_oct(gid, t->th_buf.gid, 8);
+	int_to_oct(0, t->th_buf.gid, 8);
 }
 
 
diff --git a/lib/libtar.h b/lib/libtar.h
index 13bb82d..44e95f5 100644
--- a/lib/libtar.h
+++ b/lib/libtar.h
@@ -285,7 +285,7 @@ int oct_to_int(char *oct);
 
 /* integer to NULL-terminated string-octal conversion */
 #define int_to_oct(num, oct, octlen) \
-	snprintf((oct), (octlen), "%*lo ", (octlen) - 2, (unsigned long)(num))
+	snprintf((oct), (octlen), "%0*lo", (octlen) - 1, (unsigned long)(num))
 
 /* integer to string-octal conversion, no NULL */
 void int_to_oct_nonull(int num, char *oct, size_t octlen);
@@ -306,4 +306,3 @@ int tar_append_tree(TAR *t, char *realdir, char *savedir);
 #endif
 
 #endif /* ! LIBTAR_H */
-
diff --git a/lib/util.c b/lib/util.c
index 11438ef..0d51700 100644
--- a/lib/util.c
+++ b/lib/util.c
@@ -156,8 +156,6 @@ oct_to_int(char *oct)
 void
 int_to_oct_nonull(int num, char *oct, size_t octlen)
 {
-	snprintf(oct, octlen, "%*lo", octlen - 1, (unsigned long)num);
+	snprintf(oct, octlen, "%0*lo", octlen - 1, (unsigned long)num);
 	oct[octlen - 1] = ' ';
 }
-
-
